#lang rhombus/static

// `definition` is a basic definitional extension: its value is executable and
// its unfolded equation simplifies later theorem statements.

import:
  "fixtures/definition_ok.rhm" as d

check d.one ~is d.succ(d.zero())
check d.identity(d.one) ~is d.one
