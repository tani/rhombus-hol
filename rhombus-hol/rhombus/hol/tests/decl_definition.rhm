#lang rhombus/static

// `definition` admits logical terms with no executable interpretation. The
// fixture's `select` definition and theorem are checked by `hol_theory`.

import:
  "fixture_load.rhm" open

check:
  instantiate_hol_fixture(ModulePath'lib("rhombus/hol/tests/fixtures/definition_ok.rhm")')
  ~completes
