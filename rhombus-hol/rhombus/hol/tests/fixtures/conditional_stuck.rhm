#lang rhombus/hol

type Nat
| zero()
| succ(pred :: Nat)

function pick(b :: Boolean, m :: Nat, n :: Nat) :: Nat:
  if b | m | n

// False: the false branch is `n`, not `m`.
theorem pick_of_false_is_wrong:
  forall (m :: Nat, n :: Nat): pick(false, m, n) === m
