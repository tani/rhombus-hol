#lang rhombus/hol
export: Nat zero succ dbl

// Character for character the same declaration as `fixtures/axiomatic_ok.rhm`
// except for the head word.
type Nat
| zero()
| succ(pred :: Nat)

function dbl(n :: Nat) :: Nat:
  match n
  | zero(): zero()
  | succ(k): succ(succ(dbl(k)))
