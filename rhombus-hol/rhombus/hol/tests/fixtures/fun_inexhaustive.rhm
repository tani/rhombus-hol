#lang rhombus/hol

type List(~a)
| Nil()
| Cons(head :: ~a, tail :: List(~a))

type Nat
| zero()
| succ(pred :: Nat)

// The `Cons` case is missing.
function size(xs :: List(~a)) :: Nat:
  match xs
  | Nil(): zero()
