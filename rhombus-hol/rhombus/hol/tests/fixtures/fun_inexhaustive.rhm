#lang rhombus/hol

datatype List.of(?a)
| Nil()
| Cons(head :: ?a, tail :: List.of(?a))

datatype Nat
| zero()
| succ(pred :: Nat)

// The `Cons` case is missing.
function size(xs :: List.of(?a)) :: Nat:
  match xs
  | Nil(): zero()
