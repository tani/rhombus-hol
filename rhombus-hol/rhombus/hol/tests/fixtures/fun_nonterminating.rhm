#lang rhombus/hol

type Nat
| zero()
| succ(pred :: Nat)

// Recursing on the successor rather than the predecessor: nothing shrinks.
function up(n :: Nat) :: Nat:
  match n
  | zero(): zero()
  | succ(k): up(succ(succ(k)))
