#lang rhombus/hol

type Nat
| zero()
| succ(pred :: Nat)

// A body outside the definable grammar names the offending expression instead
// of being admitted with its annotations quietly stripped.
function twice(n :: Nat) :: Nat:
  println("not a definition")
