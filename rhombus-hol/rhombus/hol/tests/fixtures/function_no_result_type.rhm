#lang rhombus/hol

type Nat
| zero()
| succ(pred :: Nat)

// `function` says the definition is meant to be logical, so a missing result
// type is an error rather than a silent fall-through to ordinary `fun`.
function twice(n :: Nat):
  match n
  | zero(): zero()
  | succ(k): succ(succ(twice(k)))
