#lang rhombus/hol

datatype Nat
| zero()
| succ(pred :: Nat)

// A rule of an inductive predicate has to conclude with that predicate.
inductive reaches(Nat)
| reaches_rule: forall (m :: Nat, n :: Nat): reaches(m) ==> m === n

def marker = "compiled"
