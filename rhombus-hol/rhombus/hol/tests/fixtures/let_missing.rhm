#lang rhombus/hol

type Nat
| zero()
| succ(pred :: Nat)

// A multi-statement body whose first statement is not `let name = expr`:
// refused outright rather than silently discarding everything but the
// last statement.
function bad(n :: Nat) :: Nat:
  n
  n
