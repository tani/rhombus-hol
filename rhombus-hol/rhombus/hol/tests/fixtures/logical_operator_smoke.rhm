#lang rhombus/hol

logical_operator (x <&> y):
  ~logic: conj(x, y)
  ~order: hol_conjunction

theorem logical_true:
  true <&> true

theorem logical_cases:
  forall (x :: Boolean, y :: Boolean):
    (x <&> y) or not (x <&> y)
proof:
  auto(~cases: [x <&> y])

expect [logical_true, logical_cases]
