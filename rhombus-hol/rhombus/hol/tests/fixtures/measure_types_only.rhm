#lang rhombus/hol

// The datatype `measure_ok.rhm` declares and nothing else, so the two
// theories' axiom counts can be compared: the difference is what that
// module's four functions cost.
export:
  Nat zero succ

type Nat
| zero()
| succ(pred :: Nat)
