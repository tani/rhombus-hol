#lang rhombus/hol

// The same type `infinity.rhm` derives, declared instead.  This exists so
// the two costs can be compared by counting, not by argument.

export: Nat zero succ
type Nat
| zero()
| succ(pred :: Nat)
