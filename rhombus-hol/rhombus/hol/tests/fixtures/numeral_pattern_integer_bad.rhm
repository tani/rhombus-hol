#lang rhombus/hol

// Only Nat has decision-tree literal patterns. Integer numerals are ordinary
// constructor applications and cannot be used as atomic patterns.
datatype Nat
| zero()
| succ(pred :: Nat)

datatype Integer
| int_from_nat(value :: Nat)

function reject_integer_pattern(x :: Integer) :: Nat
| (0): zero()
