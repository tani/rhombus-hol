#lang rhombus/hol

datatype Nat
| zero()
| succ(pred :: Nat)

// `0` and `1` cover `zero()` and `succ(zero())`; `succ(succ(_))` is left
// uncovered, and no catch-all excuses it.
function bad(n :: Nat) :: Nat:
  match n
  | 0: zero()
  | 1: succ(zero())
