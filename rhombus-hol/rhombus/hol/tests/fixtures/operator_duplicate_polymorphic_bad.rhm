#lang rhombus/hol

type Box(?element):
| box(value :: ?element)

function box_append(x :: Box(?element),
                    _y :: Box(?element)) :: Box(?element):
  x

operator Append for Box(?left):
  box_append

operator Append for Box(?right):
  box_append
