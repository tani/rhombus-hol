#lang rhombus/hol

type Box(?element):
| box(value :: ?element)

function box_append(x :: Box(?element),
                    _y :: Box(?element)) :: Box(?element):
  x

dispatch Append(Box(?left), Box(?left)) = box_append

dispatch Append(Box(?right), Box(?right)) = box_append
