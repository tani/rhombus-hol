#lang rhombus/hol

type Box.of(?element):
| box(value :: ?element)

function box_append(x :: Box.of(?element),
                    _y :: Box.of(?element)) :: Box.of(?element):
  x

dispatch Append(Box.of(?left), Box.of(?left)) = box_append

dispatch Append(Box.of(?right), Box.of(?right)) = box_append
