#lang rhombus/hol

import: "operator_extension_ok.rhm" open

notation CustomAdd (left <+> right):
  syntax (left <++> right)
  ~order: hol_addition
  ~associativity: ~left

theorem imported_custom_provider:
  forall (a :: Tiny, b :: Tiny): a <+> b === tiny_add(a, b)
