#lang rhombus/hol

import: "operator_numeral_runtime_ok.rhm" open

notation left <+> right:
  ~order: hol_addition
  bad_permutation

dispatch bad_permutation(left :: Nat, right :: Nat) = nat_add(left, left)
