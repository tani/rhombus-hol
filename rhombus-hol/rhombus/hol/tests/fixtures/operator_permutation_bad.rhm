#lang rhombus/hol

import: "operator_numeral_runtime_ok.rhm" open

operator BadPermutation:
  syntax (left <+> right)
  ~order: hol_addition
  ~associativity: ~left

operator BadPermutation for Nat:
  nat_add(left, left)
