#lang rhombus/hol
datatype List.of(?a)
| Nil()
| Cons(head :: ?a, tail :: List.of(?a))
// `_` inside a pattern is not a catch-all *clause*: nothing covers `Cons`.
function g(xs :: List.of(?a)) :: List.of(?a):
  match xs
  | Nil(): Nil()
