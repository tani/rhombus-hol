#lang rhombus/hol
type List(?a)
| Nil()
| Cons(head :: ?a, tail :: List(?a))
// `_` inside a pattern is not a catch-all *clause*: nothing covers `Cons`.
function g(xs :: List(?a)) :: List(?a):
  match xs
  | Nil(): Nil()
