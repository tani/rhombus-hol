#lang rhombus/hol
type Nat
| zero()
| succ(pred :: Nat)
// The catch-all already covers `succ`, so the last clause fires nowhere.
function f(n :: Nat) :: Nat:
  match n
  | zero(): zero()
  | _: zero()
  | succ(k): k
