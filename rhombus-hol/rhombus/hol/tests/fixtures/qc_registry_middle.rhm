#lang rhombus/hol

// Imports `Nat` and re-exports it, but does not itself use `check_property`.

export:
  Nat
  zero
  succ
  double

import: "qc_registry_provider.rhm" open

function double(n :: Nat) :: Nat:
  match n
  | zero(): zero()
  | succ(k): succ(succ(double(k)))
