#lang rhombus/hol

// Declares `Nat` and nothing else.  Nothing in this file mentions
// `check_property`; it exists only to be imported by something that is
// imported in turn.

export:
  Nat
  zero
  succ

type Nat
| zero()
| succ(pred :: Nat)
