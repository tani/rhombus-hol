#lang rhombus/hol

// Declares `Nat` and nothing else. Nothing in this file mentions
// `quickcheck`; it is reached through a transitive import.

export:
  Nat
  zero
  succ

type Nat
| zero()
| succ(pred :: Nat)
