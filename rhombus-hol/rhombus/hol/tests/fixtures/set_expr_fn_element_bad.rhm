#lang rhombus/hol

datatype Nat
| zero()
| succ(pred :: Nat)

// The condition first sees an unconstrained element type; the branch then
// resolves it to a function. Runtime equality must use that final type.
function bad_fn_set_branch(f, x :: Nat) :: Boolean:
  if Set{f}(f)
  | f(x)
  | #false
