#lang rhombus/hol

datatype Nat
| zero()
| succ(pred :: Nat)

// A set of function-typed elements would require executable function
// equality, which has no sound implementation.
function bad_fn_set_branch(b :: Boolean) :: (Nat -> Nat) -> Boolean:
  if b
  | Set{function (n :: Nat): n}
  | Set{}
