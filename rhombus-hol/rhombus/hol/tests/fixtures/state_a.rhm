#lang rhombus/hol

// The function and theorem require the logical declarations before them, so
// successful compilation exercises state threading within this module.

export: marker

type Local()
| local()

function keep(x :: Local) :: Local:
  x

theorem keep_refl:
  forall (x :: Local): keep(x) === x

def marker = "a"
