#lang rhombus/hol

// Declarations thread compile-time state through the module body.  `expect`
// asserts about that state at phase 1, so this module compiles only if the
// threading is right.

export: marker

declare alpha
declare beta
expect [alpha, beta]

def marker = "a"
