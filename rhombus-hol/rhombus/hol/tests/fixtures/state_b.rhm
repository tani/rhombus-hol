#lang rhombus/hol

// The same with different declarations.  Compiling both in one `raco make`
// is the test that state does not leak between modules: were it macro-level
// state rather than per-module phase-1 definitions, one of these two
// `expect`s would see the other's declarations.

export: marker

declare gamma
expect [gamma]

def marker = "b"
