#lang rhombus/hol

// The module's own declaration has already moved the theory on, so the
// imported one is a sibling rather than an extension.
type Colour
| red()
| blue()

use_theory "theory_base.rhm"
