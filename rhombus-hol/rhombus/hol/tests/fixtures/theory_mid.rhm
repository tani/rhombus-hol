#lang rhombus/hol

// A theory in the middle of a chain: it uses `theory_base` and adds to it.

export:
  List
  Nil
  Cons
  app
  twice

import: "theory_base.rhm" open

function twice(xs :: List(?a)) :: List(?a):
  match xs
  | Nil(): Nil()
  | Cons(x, rest): Cons(x, Cons(x, twice(rest)))
