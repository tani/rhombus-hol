#lang rhombus/hol

// Every constructor is recursive, so the type would be empty -- and HOL types
// are inhabited by assumption, so this is an inconsistency, not merely a
// useless declaration.

type Empty
| e(r :: Empty)
