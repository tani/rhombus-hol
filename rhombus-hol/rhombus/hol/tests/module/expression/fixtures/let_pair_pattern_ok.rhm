#lang rhombus/hol

// A `let` binder is not limited to a bare variable: `let Pair(a, b) = p`
// destructures through an irrefutable constructor pattern, matching the
// generalized-binder design used throughout logical binders. It proves
// outright: the destructuring reduces through the product projections'
// equations, which the base rule database carries.

export: Nat zero succ add unwrap

datatype Nat
| zero()
| succ(pred :: Nat)

function add(x :: Nat, y :: Nat) :: Nat:
  match x
  | zero(): y
  | succ(k): succ(add(k, y))

function unwrap(p :: Pair.of(Nat, Nat)) :: Nat:
  let Pair(a, b) = p
  add(a, b)

theorem unwrap_zero:
  unwrap(Pair(zero(), zero())) === zero()
