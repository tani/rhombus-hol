#lang rhombus/static

// `Set{x :: T | pred}` (and, with the type omitted, `Set{x | pred}`)
// builds an ordinary `?a -> Boolean` predicate value from a binder and a
// body, executable exactly like `Set{e1, e2, ...}`'s extensional form by
// direct application. Membership, non-membership, and omitted-type
// inference all prove.

import:
  lib("rhombus/hol/tests/fixture_load.rhm") open

def load_positive = make_hol_fixture_loader()

fun load(mp :: ModulePath):
  load_positive(mp)
  "compiled"

// Membership, omitted-type inference, and non-membership (the negated
// side: the datatype's distinctness fact discharges what beta
// normalization leaves) all prove as part of compiling one fixture.
check:
  load(ModulePath'lib("rhombus/hol/tests/module/expression/fixtures/set_comprehension_ok.rhm")')
  ~is "compiled"
