#lang rhombus/hol

export: Nat zero succ

datatype Nat
| zero()
| succ(pred :: Nat)

datatype MyInt
| pos(n :: Nat)
| negative(n :: Nat)

// An explicit type ascription resolves the same otherwise-ambiguous `0`.
theorem disambiguated_numeral:
  (0 :: Nat) === zero()
