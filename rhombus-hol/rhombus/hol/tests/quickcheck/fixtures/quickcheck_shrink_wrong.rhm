#lang rhombus/hol

import: lib("rhombus/hol/tests/quickcheck/fixtures/qc_local.rhm") open

quickcheck empty_length_wrong(
  ~samples: 200,
  ~size: 8,
  ~seed: 20240904
):
  forall (xs :: List.of(Nat)):
    length(xs) === zero()
