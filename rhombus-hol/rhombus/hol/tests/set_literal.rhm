#lang rhombus/static

// `Set{}` and `Set{e, ...}` elaborate to an ordinary predicate value
// (`elab.rhm`'s `elaborate_set_effect`): the canonical empty set elaborates
// to `λ_. #false`, and the multi-element case elaborates to the extensional
// disjunction-of-equalities predicate `λx. x === e1 or x === e2 or ...`,
// with membership in it proving outright.

import:
  "fixture_load.rhm" open

check:
  instantiate_hol_fixture(ModulePath'lib("rhombus/hol/tests/fixtures/set_literal_ok.rhm")')
  ~completes
