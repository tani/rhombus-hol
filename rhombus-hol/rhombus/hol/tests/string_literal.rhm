#lang rhombus/static

// `"..."` string literals desugar to `text(cons(code, ...))` over whatever
// datatype provides the representation (`elab.rhm`'s
// `elaborate_string_effect`). Untested until now.

import:
  "fixture_load.rhm" open

check:
  instantiate_hol_fixture(ModulePath'lib("rhombus/hol/tests/fixtures/string_literal_ok.rhm")')
  ~completes
