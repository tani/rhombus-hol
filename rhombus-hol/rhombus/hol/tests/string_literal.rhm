#lang rhombus/static

// Strings normalize to `text(cons(code, ...))` over the datatype that
// provides their representation.

import:
  "fixture_load.rhm" open

check:
  instantiate_hol_fixture(ModulePath'lib("rhombus/hol/tests/fixtures/string_literal_ok.rhm")')
  ~completes
