#lang rhombus/hol

// A Boolean literal pattern is checked for totality like any other
// constructor pattern: `#false` is a constructor of `bool` too, so
// leaving it out is a missing case, not an implicit fallthrough.

export: negate

function negate(b :: Boolean) :: Boolean:
  match b
  | #true: #false
