#lang rhombus/static

// Strings normalize to `text(cons(codepoint(code), ...))` over the nominal
// Codepoint representation, with native Nat literals as constructor fields.

import:
  lib("rhombus/hol/tests/fixture_load.rhm") open

def load_positive = make_hol_fixture_loader()

check:
  load_positive(ModulePath'lib("rhombus/hol/tests/surface/expression/fixtures/string_literal_ok.rhm")')
  ~completes
