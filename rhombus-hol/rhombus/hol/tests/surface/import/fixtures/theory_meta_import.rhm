#lang rhombus/hol

// The path is inside a block, so the prefix rule stops at `meta` and would
// drop the theory without a word.  Refused instead.
import: meta: lib("rhombus/hol/tests/surface/import/fixtures/theory_base.rhm")
