#lang rhombus/hol

datatype Nat
| zero()

notation (left <?> right) :: Boolean:
  ~order: hol_relation
  right

// Without the notation result annotation this body would have type Nat.
function rejected_result(left :: Boolean, right :: Nat) :: Nat:
  left <?> right
